# Dependencies
node_modules/

# Env (never commit, contains Reed API key)
.env
.env.local

# Claude Code local settings (per-user permissions, not shared)
.claude/settings.local.json

# OS
.DS_Store

# Logs
*.log
npm-debug.log*

# Build output (none yet, but future-proofing)
dist/
build/
